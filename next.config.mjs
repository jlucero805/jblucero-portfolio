/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        defaultLocale: "en",
        locales: ["en", "es"],
        localeDetection: false,
    }
};

export default nextConfig;
